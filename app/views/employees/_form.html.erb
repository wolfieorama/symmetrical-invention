<%= form_for(@employee) do |f| %>
  <% if @employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employee.errors.count, "error") %> prohibited this employee from being saved:</h2>

      <ul>
      <% @employee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.collection_select(:manager_id, Employee.all, :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :team %><br>
    <%= f.collection_select(:team_id, Team.all, :id, :team_name) %>
  </div>
  <div class="field">
    <%= f.label :sec_team %><br>
    <%= f.collection_select(:sec_team_id, Team.all, :id, :team_name) %>
  </div>
  <div class="field">
    <%= f.label :ter_team %><br>
    <%= f.collection_select(:ter_team_id, Team.all, :id, :team_name) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
